import { ScatterCursorsDirective } from './scatter-cursors.directive';

describe('ScatterCursorsDirective', () => {
  it('should create an instance', () => {
    const directive = new ScatterCursorsDirective();
    expect(directive).toBeTruthy();
  });
});
